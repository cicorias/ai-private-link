#cloud-config

# Install required packages
package_update: true
packages:
  - curl
runcmd:
  - cd /root
  - export AI_INSTRUMENTATION_KEY=${ai_key}
  - echo "$(pwd)"
  - [ nohup, /var/lib/cloud/instance/scripts/sendAIEvent.sh, ${ai_key}, '&' ]